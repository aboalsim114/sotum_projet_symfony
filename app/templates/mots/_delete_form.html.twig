<form method="post" action="{{ path('app_mots_delete', {'id': mot.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mot.id) }}">
    <button class="btn">Delete</button>
</form>
